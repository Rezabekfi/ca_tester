## Saves

This directory contains the saved initial states of the cellular automata.

All of the example work from first appendix in the bachelor thesis is in directory `example_data`.

In directory `rectangle_inputs` are some inputs suitable for testing the methods and in directory `real_data_showcase` are OUTPUTS of the methods on picture of windows.

